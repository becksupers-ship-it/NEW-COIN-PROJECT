import Link from 'next/link'
import { ArrowRight, Sparkles } from 'lucide-react'
import { getHomepageContent } from '@/app/actions/homepage'
import { getPublishedPosts, getPublishedStories } from '@/app/actions/content'
import { SiteShell } from '@/components/site-shell'

export const dynamic = 'force-dynamic'

export default async function Page() {
  const [content, stories, posts] = await Promise.all([getHomepageContent(), getPublishedStories(), getPublishedPosts()])
  const metrics = [[content.widowsValue, content.widowsLabel], [content.childrenValue, content.childrenLabel], [content.elderlyValue, content.elderlyLabel], [content.communitiesValue, content.communitiesLabel]]
  const values = JSON.parse(content.valuesJson || '[]') as { title: string; text: string }[]
  return <SiteShell><main><section className="mx-auto grid max-w-7xl gap-10 px-5 pb-20 pt-14 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:px-8 lg:pt-20"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full bg-[#eaf0e7] px-4 py-2 text-xs font-bold uppercase tracking-[.18em] text-[#4d7164]"><Sparkles size={14}/>{content.heroBadge}</div><h1 className="max-w-2xl text-5xl font-semibold leading-[1.03] tracking-[-.04em] sm:text-6xl lg:text-7xl">{content.heroTitle}</h1><p className="mt-7 max-w-xl text-lg leading-8 text-[#5f716a]">{content.heroDescription}</p><div className="mt-9 flex flex-wrap gap-3"><Link href="/donate" className="inline-flex items-center gap-2 rounded-full bg-[#c56b4b] px-6 py-4 text-sm font-bold text-white">{content.primaryCtaLabel}<ArrowRight size={16}/></Link><Link href="/programs" className="rounded-full border border-[#b9c9bd] px-6 py-4 text-sm font-bold">{content.secondaryCtaLabel}</Link></div></div><div className="rounded-[2.5rem] bg-[#17332d] p-8 text-[#f7f4eb]"><p className="text-sm uppercase tracking-[.18em] text-[#e5b65c]">{content.reachLabel}</p><p className="mt-4 text-7xl font-semibold text-[#e5b65c]">{content.reachValue}</p><div className="mt-8 grid grid-cols-2 gap-4">{metrics.map(([value, label]) => <div key={label} className="rounded-2xl border border-[#416256] p-4"><p className="text-3xl font-semibold">{value}</p><p className="mt-1 text-sm text-[#b9c9bd]">{label}</p></div>)}</div></div></section><section className="bg-[#eaf0e7] px-5 py-20 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#c56b4b]">Our values</p><div className="mt-8 grid gap-5 md:grid-cols-4">{values.map((value) => <article key={value.title} className="rounded-3xl bg-white p-6"><h2 className="text-xl font-semibold">{value.title}</h2><p className="mt-3 text-sm leading-6 text-[#64776e]">{value.text}</p></article>)}</div></div></section><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#c56b4b]">Stories and updates</p><h2 className="mt-3 text-4xl font-semibold">What is happening now.</h2></div><Link href="/news" className="text-sm font-bold text-[#c56b4b]">See all updates →</Link></div><div className="mt-8 grid gap-5 md:grid-cols-3">{posts.slice(0, 3).map((post) => <article key={post.id} className="rounded-3xl border border-[#d9e1d8] p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#c56b4b]">{post.type}</p><h3 className="mt-4 text-xl font-semibold">{post.title}</h3><p className="mt-3 text-sm leading-6 text-[#64776e]">{post.excerpt}</p></article>)}</div><div className="mt-8 grid gap-5 md:grid-cols-3">{stories.slice(0, 3).map((story) => <blockquote key={story.id} className="rounded-3xl bg-[#17332d] p-6 text-[#f7f4eb]">“{story.quote}”<footer className="mt-4 text-sm text-[#b9c9bd]">{story.name} · {story.category}</footer></blockquote>)}</div></section></main></SiteShell>
}
