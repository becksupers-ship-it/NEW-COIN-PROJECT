import { headers } from 'next/headers'
import { redirect } from 'next/navigation'
import Link from 'next/link'
import { auth } from '@/lib/auth'
import { db } from '@/lib/db'
import { programs } from '@/lib/db/schema'

export default async function ProgramManagerPage() { const session=await auth.api.getSession({headers:await headers()}); if(!session?.user) redirect('/admin/login'); const items=await db.select().from(programs).limit(50); return <main className="min-h-screen bg-[#f3f6f1] px-5 py-10 text-[#17332d] lg:px-10"><div className="mx-auto max-w-6xl"><Link href="/admin" className="text-sm font-bold text-[#c56b4b]">← Dashboard</Link><div className="mt-8 flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#c56b4b]">Content manager</p><h1 className="mt-2 text-4xl font-semibold">Programs</h1><p className="mt-2 text-[#64776e]">Create and maintain the work visitors see across the site.</p></div><button className="rounded-full bg-[#c56b4b] px-5 py-3 text-sm font-bold text-white">Add program</button></div><div className="mt-8 overflow-hidden rounded-3xl bg-white"><div className="grid grid-cols-[1.2fr_0.8fr_0.5fr] border-b border-[#d9e1d8] px-6 py-4 text-xs font-bold uppercase tracking-[0.12em] text-[#64776e]"><span>Program</span><span>Category</span><span>Status</span></div>{items.length?items.map(item=><div key={item.id} className="grid grid-cols-[1.2fr_0.8fr_0.5fr] items-center border-b border-[#edf1ed] px-6 py-5 last:border-0"><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-[#64776e]">{item.summary}</p></div><span className="text-sm text-[#64776e]">{item.category}</span><span className="text-sm font-semibold text-[#4d7164]">{item.active?'Active':'Archived'}</span></div>):<div className="px-6 py-16 text-center text-[#64776e]">No programs yet. Add your first program to make this page dynamic.</div>}</div></div></main> }
