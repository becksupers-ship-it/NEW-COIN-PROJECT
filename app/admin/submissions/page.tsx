import Link from 'next/link'
import { headers } from 'next/headers'
import { redirect } from 'next/navigation'
import { ArrowLeft, Download, HeartHandshake, Users } from 'lucide-react'
import { auth } from '@/lib/auth'
export const dynamic='force-dynamic'
export default async function SubmissionsManager(){const s=await auth.api.getSession({headers:await headers()});if(!s?.user)redirect('/admin/login');return <main className="min-h-screen bg-[#f3f6f1] px-5 py-10 text-[#17332d] lg:px-10"><div className="mx-auto max-w-6xl"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-[#c56b4b]"><ArrowLeft size={16}/> Dashboard</Link><div className="mt-10 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-widest text-[#c56b4b]">Submissions</p><h1 className="mt-2 text-4xl font-semibold">Donations & volunteers</h1><p className="mt-2 text-sm text-[#64776e]">Review new forms, verify transfers, and follow up with applicants.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-full border border-[#b9c9bd] px-5 py-3 text-sm font-bold"><Download size={16}/> Export CSV</button></div><div className="mt-8 grid gap-5 md:grid-cols-2"><div className="rounded-[2rem] bg-white p-8"><HeartHandshake className="text-[#c56b4b]"/><p className="mt-8 text-4xl font-semibold">0</p><p className="mt-2 font-semibold">Donation confirmations</p><p className="mt-1 text-sm text-[#789086]">No new submissions to review.</p></div><div className="rounded-[2rem] bg-white p-8"><Users className="text-[#c56b4b]"/><p className="mt-8 text-4xl font-semibold">0</p><p className="mt-2 font-semibold">Volunteer applications</p><p className="mt-1 text-sm text-[#789086]">No new applications to review.</p></div></div></div></main>}
