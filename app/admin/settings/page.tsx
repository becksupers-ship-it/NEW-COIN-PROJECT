import Link from 'next/link'
import { headers } from 'next/headers'
import { redirect } from 'next/navigation'
import { ArrowLeft, Save } from 'lucide-react'
import { auth } from '@/lib/auth'
export const dynamic='force-dynamic'
export default async function SettingsManager(){const s=await auth.api.getSession({headers:await headers()});if(!s?.user)redirect('/admin/login');return <main className="min-h-screen bg-[#f3f6f1] px-5 py-10 text-[#17332d] lg:px-10"><div className="mx-auto max-w-4xl"><Link href="/admin" className="inline-flex items-center gap-2 text-sm font-bold text-[#c56b4b]"><ArrowLeft size={16}/> Dashboard</Link><div className="mt-10"><p className="text-xs font-bold uppercase tracking-widest text-[#c56b4b]">General settings</p><h1 className="mt-2 text-4xl font-semibold">Organization settings</h1><p className="mt-2 text-sm text-[#64776e]">Keep public contact details and notification destinations current.</p></div><form className="mt-8 rounded-[2rem] bg-white p-7 shadow-sm sm:p-9"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold">Organization name<input defaultValue="Bonded Friends Outreach Initiative" className="mt-2 w-full rounded-xl border border-[#cbd8cc] p-3.5 font-normal"/></label><label className="text-sm font-semibold">Notification email<input defaultValue="bondedfriendsoutreachinitiativ@gmail.com" className="mt-2 w-full rounded-xl border border-[#cbd8cc] p-3.5 font-normal"/></label><label className="text-sm font-semibold">Phone<input className="mt-2 w-full rounded-xl border border-[#cbd8cc] p-3.5 font-normal" placeholder="Add phone number"/></label><label className="text-sm font-semibold">Address<input className="mt-2 w-full rounded-xl border border-[#cbd8cc] p-3.5 font-normal" placeholder="Add office address"/></label></div><label className="mt-5 block text-sm font-semibold">About page content<textarea className="mt-2 min-h-40 w-full rounded-xl border border-[#cbd8cc] p-3.5 font-normal" defaultValue="We are a humanitarian organization committed to restoring dignity and creating opportunity for underserved people and communities across Nigeria."/></label><button className="mt-6 inline-flex items-center gap-2 rounded-full bg-[#c56b4b] px-5 py-3 text-sm font-bold text-white"><Save size={16}/> Save settings</button></form></div></main>}
