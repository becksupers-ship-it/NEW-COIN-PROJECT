I added better price history charting and UI on the coin detail page, a background alerts checker that requests notification permission and triggers browser notifications when an alert condition is met, and made Providers wrap the AlertProvider and include the AlertsChecker so alerts are evaluated in the background. I also added the uuid dependency to package.json. Continue to monitor and I'll push further UI polish next.
